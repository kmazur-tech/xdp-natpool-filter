# These files run on Linux — force LF so a checkout on Windows plus scp never
# ships CRLF that breaks the shell/build on the target.
*.sh     text eol=lf
*.c      text eol=lf
*.h      text eol=lf
*.conf   text eol=lf
Makefile text eol=lf
